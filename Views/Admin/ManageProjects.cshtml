@using BugTracker.Helpers
@using BugTracker.Models
@model IEnumerable<UserProfileViewModel>

@{
    ViewBag.Title = "ManageProjects";
}

<h2>ManageProjects</h2>

@{
    var projHelper = new ProjectHelper();
    var roleHelper = new UserRolesHelper();
}



@using (Html.BeginForm("ManageProjects", "Admin", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{

    {
        @Html.AntiForgeryToken()

        @Html.Hidden("UserId", (string)ViewBag.UserId)

        <div class="form-group">
            @Html.Label("Projects", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.ListBox("Projects", null, htmlAttributes: new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-6">
                <input type="submit" value="Assign" class="btn btn-default" />
            </div>
        </div>

    }
}

    <hr />

    <br />

    @Html.ActionLink("Back to Dashboard", "Index", "Home")
    @Html.ActionLink("Back to User Index", "UserIndex", "Admin")
